/**
 * HelloWorld.jsst
 * @author Mustermann
 */

class HelloWorld
{
    // globals
    // i
    final int i = 1;

    // main function
    public void main() {

        int x;
        int y;
        x = 4;
        y = 5;

        if( x == y ){ f = 1; }
        else{
            f = 2;
        }

        while( x <= y){
            f(2);
        }

    }

    // add method
    public int addI(int z) {
        return z + i;
    }
}